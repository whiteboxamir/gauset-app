[project]
name = "gauset-mvp-backend"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "google-auth",
  "httpx",
  "numpy",
  "pillow",
  "python-multipart",
  "requests",
  "trimesh",
  "uvicorn",
  "vercel_blob",
]

[tool.uv]
package = false
